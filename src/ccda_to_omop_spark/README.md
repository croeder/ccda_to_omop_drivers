# 

- base_example.py is a very simple Spark example

- convert.py reads test data and uses the ccda_to_omop package to convert files in a single transform
  but the data comes up all zeros.

- read.py
